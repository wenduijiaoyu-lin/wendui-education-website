# 问对官网部署教程：GitHub Pages + Cloudflare 代理

> 上传时需要上传 `index.html`、`.nojekyll` 以及整个 `assets` 文件夹。本教程文件不用传（传了也无妨）。

## 第〇步：确认资源文件

网页首页、导航栏和案例区会调用 `assets` 文件夹里的 logo 与现场图片。请确认这些文件与 `index.html` 在同一个目录中：

- `assets/logo-gray.png`
- `assets/logo-white.png`
- `assets/hero-culture-cubes.jpg`
- 其他案例与现场图片

若后续替换图片，请保持文件名不变，或同步修改 `index.html` 里的图片路径。

## 第一部分：GitHub Pages 建站

1. 登录 GitHub → 右上角 `+` → New repository
2. 仓库名建议填 `你的用户名.github.io`（网址最短），其他名字也可以；选 **Public** → Create repository
3. 进入仓库 → Add file → **Upload files** → 把 `index.html` 和 `.nojekyll` 拖进去 → Commit changes
4. Settings → 左侧 **Pages** → Build and deployment → Branch 选 `main`，目录 `/ (root)` → Save
5. 等 1–2 分钟，页面上方会显示你的网址：
   - 仓库名是 `用户名.github.io` → 网址为 `https://用户名.github.io`
   - 其他仓库名 → 网址为 `https://用户名.github.io/仓库名/`

打开网址确认网站正常显示，第一部分完成。

## 第二部分：Cloudflare 绑定域名并代理

前提：你已有一个域名（任意注册商购买均可）。

### 1. 把域名接入 Cloudflare

1. 注册/登录 cloudflare.com → Add a domain → 输入你的域名 → 选 **Free 免费套餐**
2. Cloudflare 会给你两个 NS（域名服务器）地址，例如 `xxx.ns.cloudflare.com`
3. 回到你买域名的注册商后台，把域名的 DNS 服务器改成这两个 NS 地址
4. 等待生效（几分钟到几小时），Cloudflare 显示 Active 即接入成功

### 2. 添加 DNS 解析记录

在 Cloudflare → 你的域名 → **DNS** 页面添加：

| 类型 | 名称 | 内容 | 代理状态 |
|---|---|---|---|
| CNAME | @ | 用户名.github.io | 🟠 已代理（橙色云朵） |
| CNAME | www | 用户名.github.io | 🟠 已代理（橙色云朵） |

> 橙色云朵 = Cloudflare 代理开启，访客流量经过 Cloudflare 的 CDN，可改善国内访问并隐藏源站。

### 3. 在 GitHub 绑定自定义域名

1. 回到 GitHub 仓库 → Settings → Pages → **Custom domain** → 填你的域名（如 `wendui.com`）→ Save
2. GitHub 会自动在仓库里生成一个 `CNAME` 文件，不要删除
3. 勾选下方的 **Enforce HTTPS**（若灰色不可选，等几分钟 DNS 验证通过后再勾）

### 4. Cloudflare SSL 设置（重要，防止无限重定向）

Cloudflare → 你的域名 → **SSL/TLS** → Overview → 加密模式选 **Full（完全）**。
不要选 Flexible，否则会出现重定向循环打不开。

### 5. 验证

浏览器访问 `https://你的域名`，显示问对官网且地址栏有小锁即全部完成。
建议用手机流量（不开代理）再测一次国内访问。

## 以后如何更新网站

修改本地 `index.html` 后，到 GitHub 仓库 → 点开 `index.html` → 铅笔图标编辑，或重新 Upload files 覆盖上传 → Commit。1–2 分钟后自动生效。
若改完没变化，是 Cloudflare 缓存所致：Cloudflare → Caching → **Purge Everything** 清一下缓存。

## 常见问题

- **github.io 直连打不开但域名能开**：正常，国内直连 GitHub 不稳定，Cloudflare 代理就是解决这个的。
- **Enforce HTTPS 勾不上**：DNS 还没验证完，等 10–30 分钟再试。
- **网站打开是 404**：检查文件名必须是小写 `index.html`，且 Pages 设置里 Branch 已选 main。
- **国内访问仍然偏慢**：免费 Cloudflare 国内节点有限，属正常现象；要更快可考虑国内云 + 备案。
